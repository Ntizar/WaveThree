/**
 * Módulo de batimetría — carga y representación del fondo marino
 *
 * Fuente: GEBCO global grid (netCDF / GeoTIFF)
 * Fase 2: carga de batimetría real desde heightmaps preprocesados.
 */

export function createBathymetry(heightmapUrl) {
  // TODO: Cargar heightmap desde GEBCO → generar PlaneGeometry con desplazamiento Y
  // Usar data/processed/batimetria.bin como fuente tras preprocesar
  console.warn('🌊 Batimetría no implementada — Fase 2');
  return null;
}